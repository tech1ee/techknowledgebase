---
title: "API Design: REST, GraphQL и когда что использовать"
created: 2025-11-24
modified: 2026-01-02
type: concept
status: published
confidence: high
sources_verified: true
tags:
  - topic/architecture
  - architecture/api
  - backend/rest
  - backend/graphql
  - backend/grpc
  - type/concept
  - level/intermediate
related:
  - "[[microservices-vs-monolith]]"
  - "[[technical-debt]]"
  - "[[network-http-evolution]]"
  - "[[auth-oauth2-oidc]]"
  - "[[auth-api-service-patterns]]"
prerequisites:
  - "[[microservices-vs-monolith]]"
  - "[[network-realtime-protocols]]"
  - "[[caching-strategies]]"
  - "[[architecture-rate-limiting]]"
  - "[[api-rest-deep-dive]]"
  - "[[api-graphql-deep-dive]]"
  - "[[api-grpc-deep-dive]]"
  - "[[api-modern-patterns]]"
---

# API Design: REST, GraphQL, gRPC

> REST — простота и HTTP кэширование. GraphQL — гибкость и точные запросы. gRPC — 5-7x быстрее REST для микросервисов. В 2025 году гибридные стеки — норма: REST для публичных API, gRPC внутри платформы.

---

## Что нужно знать перед изучением

Прежде чем изучать API дизайн, убедитесь, что понимаете:

- **HTTP протокол** — методы (GET, POST), статус-коды, заголовки → [[network-http-evolution]]
- **JSON** — формат данных для REST и GraphQL
- **Клиент-сервер архитектура** — кто запрашивает, кто отвечает
- **Базовая безопасность** — зачем нужна аутентификация и авторизация

---

## Главная аналогия: API = Официант в ресторане

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      API КАК ОФИЦИАНТ                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ВЫ (Клиент)          ОФИЦИАНТ (API)          КУХНЯ (Сервер/БД)       │
│   ────────────         ──────────────          ──────────────────       │
│                                                                         │
│   📱 Приложение        🔗 REST/GraphQL/gRPC    💾 База данных           │
│                                                                         │
│   1. Смотрите меню     1. Принимает заказ      1. Готовит блюдо        │
│      (документация)       (запрос)                (обрабатывает)        │
│                                                                         │
│   2. Заказываете       2. Передаёт на кухню    2. Возвращает блюдо     │
│      (делаете запрос)     (маршрутизация)         (данные)              │
│                                                                         │
│   3. Получаете блюдо   3. Приносит ответ       3. Или сообщает         │
│      (ответ)              (response)              "блюда нет" (ошибка)  │
│                                                                         │
│   ВАЖНО: Вы НЕ идёте на кухню сами!                                    │
│   API изолирует клиента от внутренней реализации.                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Зачем это нужно

### Проблема: Неправильный выбор = переписывание через год

Неправильный выбор API-архитектуры приводит к:
- **Переписыванию** через 1-2 года
- **Performance bottlenecks** на масштабе
- **Сложностям интеграции** для клиентов

### Реальные примеры компаний

**Stripe (REST):**
- Public API для платежей
- Используют REST + JSON для простоты интеграции
- Date-based versioning: `Stripe-Version: 2024-06-20`
- Документация — золотой стандарт индустрии

**GitHub (REST + GraphQL):**
- REST API v3 — для простых операций
- GraphQL API v4 — для сложных запросов с nested data
- Rate limits: 60 req/час без auth, 5,000 с auth

**Netflix (gRPC):**
- gRPC между микросервисами
- REST/GraphQL для клиентских приложений
- Результат: миллисекундные latency внутри платформы

**Twilio (REST):**
- REST API для SMS/Voice
- Дают год предупреждения перед deprecation версии
- POST вместо PUT для обновлений (не strict REST)

### Бенчмарки 2024-2025

| Метрика | REST | GraphQL | gRPC |
|---------|------|---------|------|
| Latency | 250ms | 180ms (complex) | **25ms** |
| Throughput | 20k RPS | 15k RPS | **50k RPS** |
| CPU usage | Baseline | +20% | **-40%** |
| Payload size | 100% (JSON) | 70-100% | **10-30% (Protobuf)** |

### Hyrum's Law — Закон неизбежности

> "При достаточном количестве пользователей API, неважно что вы обещаете в контракте — от всех наблюдаемых поведений системы кто-то будет зависеть."

**Что это значит на практике:**
```
Вы документировали:
  "Поле created_at содержит дату создания"

Вы НЕ документировали:
  "Формат даты: ISO 8601"
  "Timezone: UTC"
  "Поле всегда присутствует"

Но какой-то клиент уже зависит от того, что:
- Формат всегда "2024-01-15T10:30:00Z"
- Timezone всегда UTC
- Поле никогда не null

Вы решили "улучшить" формат → сломали этого клиента.
```

**Следствия:**
- Даже недокументированное поведение становится API
- Breaking changes ломают больше чем ожидалось
- Backwards compatibility важнее новых фич

---

## Терминология (с аналогиями)

| Термин | Значение | Аналогия |
|--------|----------|----------|
| **API** | Application Programming Interface | Меню ресторана |
| **Endpoint** | URL для запроса | Отдел в магазине (молочный, мясной) |
| **Resource** | Сущность в REST (users, orders) | Товар на полке |
| **HTTP verb** | GET, POST, PUT, DELETE | Действие: взять, положить, заменить, выбросить |
| **Request** | Запрос от клиента | Заказ официанту |
| **Response** | Ответ от сервера | Блюдо на тарелке |
| **Status code** | Код результата (200, 404, 500) | "Готово", "Нет в наличии", "Пожар на кухне" |
| **Payload** | Данные в запросе/ответе | Содержимое заказа |
| **Schema** | Описание структуры данных | Рецепт блюда |
| **OAuth** | Протокол авторизации | Пропускная система |
| **JWT** | JSON Web Token | Пропуск с QR-кодом |
| **Rate limit** | Ограничение запросов | "Не более 3 блюд в час" |

---

## Три парадигмы: когда какую выбирать

### REST — "Ресурсы и HTTP-глаголы"

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              REST                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   Аналогия: Библиотека с каталогом                                     │
│                                                                         │
│   • Каждая книга (ресурс) имеет уникальный номер (ID)                  │
│   • Действия: взять (GET), вернуть (DELETE), заказать (POST)           │
│   • Каталог (URL) организован логично: /полка/ряд/книга                │
│                                                                         │
│   GET    /users/123        → Получить пользователя 123                 │
│   POST   /users            → Создать нового пользователя               │
│   PUT    /users/123        → Полностью обновить пользователя 123       │
│   PATCH  /users/123        → Частично обновить пользователя 123        │
│   DELETE /users/123        → Удалить пользователя 123                  │
│                                                                         │
│   ✓ Простота — любой разработчик поймёт                               │
│   ✓ HTTP кэширование из коробки                                        │
│   ✓ Стандарты (OpenAPI, JSON Schema)                                   │
│                                                                         │
│   ✗ Over-fetching: GET /users/123 возвращает ВСЕ поля                  │
│   ✗ Under-fetching: нужно 3 запроса для связанных данных              │
│   ✗ Множество endpoints                                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### GraphQL — "Запроси только то, что нужно"

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            GraphQL                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   Аналогия: Шведский стол                                              │
│                                                                         │
│   • Вы сами выбираете что положить на тарелку                          │
│   • Берёте только то, что хотите съесть                                │
│   • Один подход к столу = один запрос                                  │
│                                                                         │
│   query {                                                               │
│     user(id: 123) {                                                     │
│       name            ← Только имя                                      │
│       email           ← и email                                         │
│       posts {         ← Вложенные данные                               │
│         title         ← в одном запросе!                                │
│       }                                                                 │
│     }                                                                   │
│   }                                                                     │
│                                                                         │
│   ✓ Точные запросы — никакого over-fetching                           │
│   ✓ Один запрос для связанных данных                                   │
│   ✓ Строгая типизация (schema)                                         │
│                                                                         │
│   ✗ Сложнее кэшировать (один endpoint для всего)                       │
│   ✗ N+1 проблема на сервере (решается DataLoader)                      │
│   ✗ Выше порог входа для команды                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### gRPC — "Binary protocol для микросервисов"

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              gRPC                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   Аналогия: Внутренняя связь на заводе                                 │
│                                                                         │
│   • Станки "говорят" на специальном языке (Protocol Buffers)           │
│   • Очень быстро, но люди не понимают без "переводчика"                │
│   • Двусторонняя связь (streaming)                                     │
│                                                                         │
│   // users.proto                                                        │
│   service UserService {                                                 │
│     rpc GetUser(UserRequest) returns (User);                           │
│     rpc StreamUsers(Empty) returns (stream User);  // Streaming!       │
│   }                                                                     │
│                                                                         │
│   message User {                                                        │
│     int64 id = 1;                                                       │
│     string name = 2;                                                    │
│   }                                                                     │
│                                                                         │
│   ✓ 5-7x быстрее REST (binary + HTTP/2)                                │
│   ✓ Bidirectional streaming                                             │
│   ✓ Автогенерация клиентов на 10+ языках                               │
│                                                                         │
│   ✗ Не работает напрямую в браузерах (нужен proxy)                     │
│   ✗ Сложнее дебажить (бинарный формат)                                 │
│   ✗ Выше порог входа                                                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Матрица выбора

| Сценарий | Рекомендация | Почему |
|----------|--------------|--------|
| Public API, 3rd party devs | **REST** + OpenAPI | Стандарты, кэширование, простота |
| Mobile app с сложным UI | **GraphQL** | Точные запросы, один roundtrip |
| Микросервисы, внутренняя связь | **gRPC** | 5-7x быстрее, streaming, типизация |
| Real-time, bidirectional | **gRPC** / WebSocket | Streaming |
| Event-driven, webhooks | **REST** + AsyncAPI | Простота, стандарты |
| MVP, быстрый старт | **REST** | Минимальный порог входа |

---

## REST: делаем правильно

### Именование ресурсов

```
❌ Плохо (глаголы в URL):
GET  /getUsers              ← GET уже означает "получить"
GET  /user/get/123          ← Избыточно
POST /createNewUser         ← POST уже означает "создать"
GET  /users/123/getOrders   ← Избыточно

✅ Хорошо (существительные, множественное число):
GET    /users               → Список пользователей
GET    /users/123           → Конкретный пользователь
POST   /users               → Создать пользователя
PUT    /users/123           → Обновить пользователя
DELETE /users/123           → Удалить пользователя
GET    /users/123/orders    → Заказы пользователя

Правила:
────────
1. Существительные во множественном числе: /users, /products, /orders
2. Иерархия через вложенность: /users/123/orders/456
3. HTTP-глагол определяет действие, не URL
4. Не более 2-3 уровней вложенности
5. Используйте kebab-case: /user-profiles, не /userProfiles
```

### HTTP-статусы: шпаргалка

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      HTTP STATUS CODES                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   2xx — УСПЕХ (всё хорошо)                                             │
│   ────────────────────────                                              │
│   200 OK              Успешный GET, PUT, PATCH                         │
│   201 Created         POST успешно создал ресурс                       │
│                       Вернуть Location header с URL нового ресурса     │
│   204 No Content      DELETE успешен, тело пустое                      │
│                                                                         │
│   4xx — ОШИБКА КЛИЕНТА (вы накосячили)                                 │
│   ─────────────────────────────────────                                │
│   400 Bad Request     Невалидный JSON, отсутствуют обязательные поля   │
│   401 Unauthorized    Нет токена или токен невалидный                  │
│                       "Кто вы? Представьтесь!"                         │
│   403 Forbidden       Токен валидный, но нет прав                      │
│                       "Я вас знаю, но вам сюда нельзя"                 │
│   404 Not Found       Ресурс не существует                             │
│   409 Conflict        Конфликт (дубликат email, race condition)        │
│   422 Unprocessable   Валидация не прошла (email невалидный формат)    │
│   429 Too Many Req    Rate limit превышен                              │
│                                                                         │
│   5xx — ОШИБКА СЕРВЕРА (мы накосячили)                                 │
│   ────────────────────────────────────                                 │
│   500 Internal Error  Что-то сломалось на сервере                      │
│   502 Bad Gateway     Upstream сервис недоступен                       │
│   503 Service Unavail Сервис перегружен, попробуйте позже              │
│   504 Gateway Timeout Upstream сервис не ответил вовремя               │
│                                                                         │
│   ВАЖНО: 401 vs 403                                                    │
│   ─────────────────                                                    │
│   401: "Я не знаю кто вы" (нет аутентификации)                         │
│   403: "Я знаю кто вы, но вам нельзя" (нет авторизации)                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Формат ошибок (консистентный)

```json
// ✅ Хороший формат ошибки
{
  "error": {
    "code": "VALIDATION_ERROR",        // Машиночитаемый код
    "message": "Validation failed",     // Человекочитаемое сообщение
    "details": [                        // Детали для отладки
      {
        "field": "email",
        "message": "Invalid email format",
        "value": "not-an-email"
      },
      {
        "field": "age",
        "message": "Must be at least 18",
        "value": 15
      }
    ],
    "request_id": "req_abc123",         // Для поиска в логах
    "documentation_url": "https://api.example.com/docs/errors#VALIDATION_ERROR"
  }
}

// ❌ Плохой формат (неконсистентный)
{
  "error": "Something went wrong"  // Бесполезно для клиента
}
```

### Пагинация

```json
// =====================================================================
// OFFSET-BASED (простая, но проблемная на больших данных)
// =====================================================================
//
// Проблема: При offset=100000 БД должна пропустить 100000 записей
// Это ОЧЕНЬ медленно на больших таблицах

GET /users?offset=100&limit=20

// Response
{
  "data": [...],
  "pagination": {
    "total": 1523,
    "offset": 100,
    "limit": 20
  }
}

// =====================================================================
// CURSOR-BASED (быстрая, рекомендуется)
// =====================================================================
//
// Cursor = зашифрованный указатель на последнюю запись
// БД использует индекс, не нужно пропускать записи
//
// Как это работает:
// 1. Клиент запрашивает первую страницу
// 2. Сервер возвращает данные + next_cursor (закодированный ID последней записи)
// 3. Клиент запрашивает следующую страницу с этим cursor
// 4. Сервер декодирует cursor и делает WHERE id > cursor_id LIMIT 20

GET /users?cursor=eyJpZCI6MTAwfQ&limit=20
//                ↑ Base64 encoded {"id": 100}

// Response
{
  "data": [...],
  "pagination": {
    "next_cursor": "eyJpZCI6MTIwfQ",  // Cursor для следующей страницы
    "has_more": true
  }
}

// Сравнение:
//
// Offset: SELECT * FROM users OFFSET 100000 LIMIT 20
//         ↑ Медленно: сканирует 100020 записей
//
// Cursor: SELECT * FROM users WHERE id > 100000 LIMIT 20
//         ↑ Быстро: использует индекс
```

### Версионирование

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    СТРАТЕГИИ ВЕРСИОНИРОВАНИЯ                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. URL PATH (рекомендуется для публичных API)                         │
│  ─────────────────────────────────────────────                         │
│                                                                         │
│  GET /v1/users                                                          │
│  GET /v2/users                                                          │
│                                                                         │
│  ✓ Очевидно, легко тестировать                                         │
│  ✓ Можно кэшировать разные версии отдельно                             │
│  ✗ "Нарушает" REST (версия не часть ресурса)                           │
│                                                                         │
│  Кто использует: GitHub, Google                                        │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  2. HEADER (Stripe подход)                                             │
│  ─────────────────────────                                             │
│                                                                         │
│  GET /users                                                             │
│  Stripe-Version: 2024-06-20                                            │
│                                                                         │
│  ✓ URL чистый, версия = деталь реализации                              │
│  ✓ Гибко: можно менять версию без изменения URL                        │
│  ✗ Менее очевидно для разработчиков                                    │
│                                                                         │
│  Кто использует: Stripe (date-based), GitHub (X-GitHub-Api-Version)    │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  3. QUERY PARAMETER (не рекомендуется)                                 │
│  ────────────────────────────────────                                  │
│                                                                         │
│  GET /users?version=2                                                   │
│                                                                         │
│  ✗ Портит кэширование                                                  │
│  ✗ Легко забыть добавить                                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

BEST PRACTICE: Избегайте breaking changes!
──────────────────────────────────────────

Вместо:
  • Удаления полей → Deprecation warning, потом удаление через 6-12 мес
  • Изменения типа → Новое поле с новым именем
  • Изменения URL → Redirect со старого на новый

Stripe даёт своим клиентам выбирать версию API в UI аккаунта.
Это позволяет им не ломать существующие интеграции при обновлениях.
```

---

## GraphQL: избегаем ловушек

### N+1 проблема (критично!)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       N+1 ПРОБЛЕМА                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Запрос:                                                                │
│  ────────                                                               │
│  query {                                                                │
│    users {          ← Получить всех пользователей                       │
│      name                                                               │
│      posts {        ← Для КАЖДОГО пользователя получить посты          │
│        title                                                            │
│      }                                                                  │
│    }                                                                    │
│  }                                                                      │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  ❌ БЕЗ DataLoader (N+1 запросов):                                      │
│                                                                         │
│  SELECT * FROM users                     → 1 запрос (получили 100 users)│
│  SELECT * FROM posts WHERE user_id = 1   → 1 запрос                     │
│  SELECT * FROM posts WHERE user_id = 2   → 1 запрос                     │
│  SELECT * FROM posts WHERE user_id = 3   → 1 запрос                     │
│  ...                                                                    │
│  SELECT * FROM posts WHERE user_id = 100 → 1 запрос                     │
│                                                                         │
│  Итого: 1 + 100 = 101 запрос к БД! 🔥                                  │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  ✅ С DataLoader (2 запроса):                                           │
│                                                                         │
│  SELECT * FROM users                                   → 1 запрос       │
│  SELECT * FROM posts WHERE user_id IN (1,2,3,...,100)  → 1 запрос       │
│                                                                         │
│  Итого: 2 запроса! ⚡                                                   │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  DataLoader собирает все ID за один "тик" event loop                   │
│  и делает один batch запрос вместо N отдельных.                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

```javascript
// =============================================================
// DATALOADER: Решение N+1 проблемы
// =============================================================

const DataLoader = require('dataloader');

// Создаём loader для постов
const postLoader = new DataLoader(async (userIds) => {
  // userIds = [1, 2, 3, ..., 100] — все ID, собранные за один тик

  // Один запрос вместо 100
  const posts = await db.query(
    'SELECT * FROM posts WHERE user_id IN ($1)',
    [userIds]
  );

  // DataLoader требует вернуть результаты в том же порядке, что и входные ID
  const postsByUserId = {};
  posts.forEach(post => {
    if (!postsByUserId[post.user_id]) {
      postsByUserId[post.user_id] = [];
    }
    postsByUserId[post.user_id].push(post);
  });

  // Возвращаем в порядке userIds
  return userIds.map(id => postsByUserId[id] || []);
});

// Использование в resolver
const resolvers = {
  User: {
    posts: (user) => postLoader.load(user.id)
    // Каждый вызов .load() не делает запрос сразу
    // DataLoader собирает все ID и делает один batch запрос
  }
};
```

### Защита от злоупотреблений

```javascript
// =============================================================
// ЗАЩИТА GRAPHQL: Ограничение глубины и сложности
// =============================================================

// Проблема: Злоумышленник может сделать запрос
// query {
//   users {
//     posts {
//       author {
//         posts {
//           author {
//             posts {
//               author { ... }  // Бесконечная вложенность
//             }
//           }
//         }
//       }
//     }
//   }
// }

const depthLimit = require('graphql-depth-limit');
const costAnalysis = require('graphql-cost-analysis');

const server = new ApolloServer({
  validationRules: [
    // Ограничение глубины вложенности
    depthLimit(5),  // Максимум 5 уровней

    // Ограничение "стоимости" запроса
    costAnalysis({
      maximumCost: 1000,       // Максимальная "стоимость"
      defaultCost: 1,          // Стоимость простого поля
      scalarCost: 1,           // Стоимость скаляра
      objectCost: 2,           // Стоимость объекта
      listFactor: 10           // Множитель для списков
    })
  ]
});

// Пример расчёта стоимости:
// query {
//   users {        → 2 (object) × 10 (list) = 20
//     name         → 1
//     posts {      → 2 × 10 = 20
//       title      → 1
//     }
//   }
// }
// Итого: 20 + 1 + 20 + 1 = 42 (допустимо)
```

---

## Безопасность: OAuth 2.0

### Главная аналогия: Гостиница с ключ-картами

```
┌─────────────────────────────────────────────────────────────────────────┐
│               OAuth 2.0 = ГОСТИНИЦА С КЛЮЧ-КАРТАМИ                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Вы (Resource Owner) — гость гостиницы                                 │
│  Приложение (Client) — ваш друг, которому нужен доступ в номер         │
│  Auth Server — ресепшен гостиницы                                      │
│  Resource Server — ваш номер с вещами                                  │
│                                                                         │
│  СИТУАЦИЯ:                                                             │
│  Ваш друг хочет взять из вашего номера зонтик, пока вас нет.           │
│  Вы НЕ хотите давать ему свой главный ключ (пароль).                   │
│                                                                         │
│  РЕШЕНИЕ (OAuth):                                                       │
│  1. Друг идёт на ресепшен: "Мне нужен доступ к номеру 123"             │
│  2. Ресепшен звонит вам: "Друг хочет доступ. Разрешить?"               │
│  3. Вы: "Да, но только для взятия вещей, не для ночёвки"               │
│  4. Ресепшен даёт другу временную карточку (access token)              │
│  5. Карточка работает 1 час и только открывает дверь                   │
│                                                                         │
│  РЕЗУЛЬТАТ:                                                             │
│  ✓ Друг получил доступ                                                 │
│  ✓ Вы не давали свой пароль                                            │
│  ✓ Доступ ограничен по времени и scope                                 │
│  ✓ Вы можете отозвать доступ в любой момент                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Authorization Code Flow — пошагово

```
┌─────────────────────────────────────────────────────────────────────────┐
│               AUTHORIZATION CODE FLOW — ПО ШАГАМ                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌────────┐      ┌────────────┐      ┌──────────────┐      ┌────────┐ │
│   │  User  │      │   Client   │      │  Auth Server │      │  API   │ │
│   │(Browser│      │ (Your App) │      │  (Google/    │      │Resource│ │
│   │        │      │            │      │   GitHub)    │      │ Server │ │
│   └───┬────┘      └─────┬──────┘      └──────┬───────┘      └───┬────┘ │
│       │                 │                     │                  │      │
│       │ 1. "Войти через Google"              │                  │      │
│       │─────────────────>                     │                  │      │
│       │                 │                     │                  │      │
│       │                 │ 2. Redirect to Auth Server             │      │
│       │<─────────────────────────────────────>                   │      │
│       │                 │                     │                  │      │
│       │ 3. User видит: "Приложение X хочет доступ к вашему email"│      │
│       │    "Разрешить?"                       │                  │      │
│       │─────────────────────────────────────>│                   │      │
│       │                 │                     │                  │      │
│       │ 4. User нажимает "Разрешить"          │                  │      │
│       │─────────────────────────────────────>│                   │      │
│       │                 │                     │                  │      │
│       │ 5. Auth Server redirect с Authorization Code             │      │
│       │<─────────────────────────────────────│                   │      │
│       │                 │                     │                  │      │
│       │ 6. Browser redirect к Client с Code  │                   │      │
│       │─────────────────>                     │                  │      │
│       │                 │                     │                  │      │
│       │                 │ 7. Client обменивает Code + Secret     │      │
│       │                 │    на Access Token                     │      │
│       │                 │─────────────────────>                  │      │
│       │                 │                     │                  │      │
│       │                 │ 8. Auth Server возвращает:             │      │
│       │                 │    - Access Token (живёт 1 час)        │      │
│       │                 │    - Refresh Token (живёт 30 дней)     │      │
│       │                 │<─────────────────────                  │      │
│       │                 │                     │                  │      │
│       │                 │ 9. Client делает запрос с Access Token │      │
│       │                 │────────────────────────────────────────>      │
│       │                 │                     │                  │      │
│       │                 │ 10. API возвращает данные              │      │
│       │                 │<────────────────────────────────────────      │
│       │                 │                     │                  │      │
│                                                                         │
│   ПОЧЕМУ Authorization Code, а не сразу токен?                         │
│   ─────────────────────────────────────────────                        │
│   Code передаётся через браузер (небезопасно) → одноразовый            │
│   Token обменивается server-to-server → безопасно                      │
│                                                                         │
│   PKCE (Proof Key for Code Exchange) — дополнительная защита           │
│   для мобильных приложений и SPA (где нет client_secret)               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### JWT: Почему каждая практика важна

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    JWT SECURITY: ПОЧЕМУ ЭТО ВАЖНО                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  JWT = Header.Payload.Signature (три части через точку)                │
│  eyJhbGciOiJSUzI1NiJ9.eyJzdWIiOiIxMjMifQ.signature_here                │
│                                                                         │
│  Структура:                                                             │
│  ──────────                                                             │
│  {                                                                      │
│    "header": {                                                          │
│      "alg": "RS256",  // Алгоритм подписи                              │
│      "typ": "JWT"                                                       │
│    },                                                                   │
│    "payload": {                                                         │
│      "sub": "user_123",       // Subject (кто)                         │
│      "iat": 1700000000,       // Issued At (когда выдан)               │
│      "exp": 1700003600,       // Expiration (когда истекает)           │
│      "scope": "read:users"    // Разрешения                            │
│    },                                                                   │
│    "signature": "..."         // Криптографическая подпись             │
│  }                                                                      │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ПРАКТИКА 1: Короткий срок жизни (15 мин - 1 час)                      │
│  ─────────────────────────────────────────────────                     │
│                                                                         │
│  ПОЧЕМУ: JWT нельзя отозвать после выдачи!                             │
│                                                                         │
│  Сценарий:                                                              │
│  1. Пользователь залогинился, получил JWT на 30 дней                   │
│  2. Пользователь уволился из компании                                   │
│  3. Админ деактивировал аккаунт                                         │
│  4. JWT ВСЁ ЕЩЁ РАБОТАЕТ 30 дней! 🔥                                   │
│                                                                         │
│  Решение:                                                               │
│  • Access Token: 15 минут - 1 час                                       │
│  • Refresh Token: 7-30 дней (хранится в secure httpOnly cookie)        │
│  • При каждом refresh — проверяем актуальность пользователя            │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ПРАКТИКА 2: Используйте RS256, не HS256                               │
│  ───────────────────────────────────────                               │
│                                                                         │
│  HS256 (HMAC):                                                          │
│  • Один секретный ключ для подписи И проверки                          │
│  • Если ключ утёк — можно подделать любой токен                        │
│  • Ключ должен быть на всех серверах → больше точек утечки             │
│                                                                         │
│  RS256 (RSA):                                                           │
│  • Приватный ключ подписывает (только на auth server)                  │
│  • Публичный ключ проверяет (можно раздать всем)                       │
│  • Утечка публичного ключа ≠ возможность подделки                      │
│                                                                         │
│  Атака "Algorithm Confusion":                                           │
│  Злоумышленник меняет header: {"alg": "HS256"}                         │
│  И подписывает публичным ключом как HMAC                               │
│  Если сервер не проверяет алгоритм строго — токен примется!            │
│                                                                         │
│  Защита: ВСЕГДА явно указывайте ожидаемый алгоритм при верификации     │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ПРАКТИКА 3: Не храните sensitive данные в JWT                         │
│  ─────────────────────────────────────────────                         │
│                                                                         │
│  JWT НЕ зашифрован, только подписан!                                   │
│  Любой может декодировать Base64 и увидеть payload.                    │
│                                                                         │
│  ❌ НЕ хранить:                                                         │
│  • Пароли (очевидно)                                                   │
│  • Персональные данные (GDPR нарушение)                                │
│  • Секреты, API ключи                                                  │
│  • Данные кредитных карт                                               │
│                                                                         │
│  ✅ Хранить:                                                            │
│  • User ID (sub claim)                                                 │
│  • Роли / Permissions                                                   │
│  • Tenant ID (для multi-tenant)                                         │
│  • Expiration                                                           │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  УЯЗВИМОСТЬ: "none" Algorithm Attack                                   │
│  ─────────────────────────────────                                     │
│                                                                         │
│  JWT спецификация разрешает алгоритм "none" (без подписи)              │
│                                                                         │
│  Атака:                                                                 │
│  1. Злоумышленник берёт легитимный JWT                                 │
│  2. Меняет header: {"alg": "none"}                                     │
│  3. Меняет payload: {"sub": "admin", "role": "superuser"}              │
│  4. Убирает signature                                                   │
│  5. Если сервер не проверяет — токен принимается                       │
│                                                                         │
│  Защита: Явный whitelist алгоритмов, НИКОГДА не принимать "none"       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

```javascript
// =============================================================
// JWT ВЕРИФИКАЦИЯ: Правильный способ
// =============================================================

const jwt = require('jsonwebtoken');

// ❌ ОПАСНО: Алгоритм берётся из токена
const decoded = jwt.verify(token, publicKey);  // ПЛОХО!

// ✅ БЕЗОПАСНО: Явно указываем разрешённые алгоритмы
const decoded = jwt.verify(token, publicKey, {
  algorithms: ['RS256'],        // Только RS256!
  issuer: 'https://auth.example.com',  // Проверяем issuer
  audience: 'my-api',           // Проверяем audience
  clockTolerance: 30            // 30 сек допуск на расхождение часов
});

// Дополнительные проверки после верификации:
if (decoded.exp < Date.now() / 1000) {
  throw new Error('Token expired');
}

// Проверка, что пользователь ещё активен (не заблокирован)
const user = await db.users.findById(decoded.sub);
if (!user || user.status !== 'active') {
  throw new Error('User not active');
}
```

### Rate Limiting

```javascript
// =============================================================
// RATE LIMITING: Защита от злоупотреблений
// =============================================================

const rateLimit = require('express-rate-limit');

// Общий лимит для всех endpoints
const generalLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,  // 15 минут
  max: 100,                   // 100 запросов за окно

  // Ответ при превышении лимита
  message: {
    error: {
      code: 'RATE_LIMIT_EXCEEDED',
      message: 'Too many requests, please try again later',
      retryAfter: 15 * 60     // Секунд до сброса
    }
  },

  // Стандартные заголовки (RFC 6585)
  standardHeaders: true,     // RateLimit-* headers
  legacyHeaders: false,      // Отключаем X-RateLimit-*

  // Как идентифицировать клиента
  keyGenerator: (req) => {
    // По API ключу если есть, иначе по IP
    return req.headers['x-api-key'] || req.ip;
  }
});

// Строгий лимит для auth endpoints (защита от brute force)
const authLimiter = rateLimit({
  windowMs: 60 * 60 * 1000,  // 1 час
  max: 5,                     // 5 попыток
  message: {
    error: {
      code: 'AUTH_RATE_LIMIT',
      message: 'Too many login attempts, account locked for 1 hour'
    }
  }
});

app.use('/api/', generalLimiter);
app.use('/api/auth/login', authLimiter);

// Response headers:
// RateLimit-Limit: 100
// RateLimit-Remaining: 42
// RateLimit-Reset: 1700000000
```

---

## Документация: OpenAPI 3.2

```yaml
# =============================================================
# OPENAPI SPECIFICATION
# =============================================================
# Каждая секция объяснена комментариями

openapi: 3.2.0

info:
  title: Users API
  version: 1.0.0
  description: |
    API для управления пользователями.

    ## Аутентификация
    Все запросы требуют Bearer token в header:
    ```
    Authorization: Bearer <your_token>
    ```

    ## Rate Limits
    - 100 запросов в 15 минут для стандартных endpoints
    - 5 попыток в час для /auth/login

  contact:
    name: API Support
    email: api@example.com
  license:
    name: MIT

servers:
  - url: https://api.example.com/v1
    description: Production
  - url: https://staging-api.example.com/v1
    description: Staging

# Определение security схемы
security:
  - BearerAuth: []  # Требовать авторизацию по умолчанию

paths:
  /users:
    get:
      summary: Список пользователей
      description: |
        Возвращает пагинированный список пользователей.
        Поддерживает cursor-based пагинацию.
      tags:
        - Users
      parameters:
        - name: cursor
          in: query
          description: Cursor для пагинации (из предыдущего ответа)
          schema:
            type: string
            example: eyJpZCI6MTAwfQ
        - name: limit
          in: query
          description: Количество результатов на странице
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
      responses:
        '200':
          description: Успешный ответ
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserListResponse'
              example:
                data:
                  - id: "123e4567-e89b-12d3-a456-426614174000"
                    email: "user@example.com"
                    name: "John Doe"
                pagination:
                  next_cursor: "eyJpZCI6MTIwfQ"
                  has_more: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/RateLimitExceeded'

    post:
      summary: Создать пользователя
      tags:
        - Users
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRequest'
            example:
              email: "new@example.com"
              name: "Jane Doe"
              password: "securePassword123"
      responses:
        '201':
          description: Пользователь создан
          headers:
            Location:
              description: URL созданного ресурса
              schema:
                type: string
                example: /v1/users/123e4567-e89b-12d3-a456-426614174000
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Пользователь с таким email уже существует
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error:
                  code: DUPLICATE_EMAIL
                  message: User with this email already exists

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    User:
      type: object
      required:
        - id
        - email
      properties:
        id:
          type: string
          format: uuid
          description: Уникальный идентификатор
        email:
          type: string
          format: email
          description: Email пользователя
        name:
          type: string
          description: Имя пользователя
        created_at:
          type: string
          format: date-time
          description: Дата создания (ISO 8601)

    CreateUserRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
        name:
          type: string
          minLength: 1
          maxLength: 100
        password:
          type: string
          format: password
          minLength: 8

    UserListResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/User'
        pagination:
          $ref: '#/components/schemas/Pagination'

    Pagination:
      type: object
      properties:
        next_cursor:
          type: string
          nullable: true
        has_more:
          type: boolean

    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: array
              items:
                type: object

  responses:
    Unauthorized:
      description: Не авторизован
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error:
              code: UNAUTHORIZED
              message: Invalid or missing authentication token

    BadRequest:
      description: Невалидные данные
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    RateLimitExceeded:
      description: Превышен лимит запросов
      headers:
        RateLimit-Limit:
          schema:
            type: integer
          description: Максимум запросов за период
        RateLimit-Reset:
          schema:
            type: integer
          description: Unix timestamp когда лимит сбросится
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
          example:
            error:
              code: RATE_LIMIT_EXCEEDED
              message: Too many requests
              retryAfter: 900
```

---

## Глубже по каждой технологии

Этот материал — обзор и сравнение. Для глубокого погружения в каждую технологию:

| Технология | Deep-dive материал | Ключевые темы |
|------------|-------------------|---------------|
| **REST** | [[api-rest-deep-dive]] | Fielding constraints, Richardson Maturity Model, HATEOAS, идемпотентность, ETags, Content Negotiation |
| **GraphQL** | [[api-graphql-deep-dive]] | SDL, execution model, Apollo Federation, subscriptions, клиентское кэширование, persisted queries |
| **gRPC** | [[api-grpc-deep-dive]] | Protocol Buffers encoding, 4 паттерна коммуникации, HTTP/2 mapping, deadline propagation, load balancing |
| **Современные паттерны** | [[api-modern-patterns]] | tRPC, Webhooks, API Gateway, BFF, SOAP (исторический контекст), JSON:API |

---

## Распространённые ошибки

| Ошибка | Почему плохо | Как правильно |
|--------|--------------|---------------|
| Глаголы в URL | `/getUsers` — GET уже означает "получить" | `/users` с HTTP методом |
| 200 для ошибок | Клиент не знает что ошибка | Правильные статус-коды |
| JWT на 30 дней | Нельзя отозвать, security риск | 15 мин access + refresh |
| Пароль в JWT | JWT не зашифрован, все видят | Только ID и roles |
| HS256 везде | Один ключ для всех серверов | RS256 с key rotation |
| Без rate limiting | DoS, brute force атаки | Limiter на все endpoints |
| Breaking changes | Ломают всех клиентов | Deprecation + новая версия |
| Offset пагинация | Медленно на больших данных | Cursor-based |

---

## Actionable: чеклист API

```
ПЕРЕД РАЗРАБОТКОЙ:
□ Выбран подход (REST/GraphQL/gRPC) с обоснованием
□ Определена схема аутентификации (OAuth 2.0)
□ Продумана стратегия версионирования
□ Определены rate limits

СТРУКТУРА:
□ Консистентное именование ресурсов (существительные, множ. число)
□ Правильные HTTP-статусы (не 200 для ошибок!)
□ Единый формат ошибок с кодами и деталями
□ Cursor-based пагинация для списков

БЕЗОПАСНОСТЬ:
□ OAuth 2.0 + JWT с коротким сроком жизни
□ RS256 алгоритм (не HS256)
□ Rate limiting на всех endpoints
□ Input validation (OpenAPI schema)
□ HTTPS only, HSTS header

ДОКУМЕНТАЦИЯ:
□ OpenAPI 3.x спецификация
□ Примеры запросов/ответов для каждого endpoint
□ Описание ошибок и как их обрабатывать
□ Changelog с датами изменений
□ Deprecation policy (сколько времени до удаления)

МОНИТОРИНГ:
□ Логирование всех запросов
□ Метрики latency (P50, P95, P99)
□ Alerting на 5xx ошибки
□ Dashboard с rate limit hits
```

---

## Связи

- API как часть архитектуры: [[microservices-vs-monolith]]
- Документирование API как часть качества: [[technical-debt]]
- HTTP протокол: [[network-http-evolution]]
- Rate Limiting подробно: [[architecture-rate-limiting]]
- Кэширование API: [[caching-strategies]]

---

## Источники

| # | Источник | Тип | Достоверность | Вклад |
|---|----------|-----|---------------|-------|
| 1 | [Microsoft Azure: API Design Best Practices](https://learn.microsoft.com/en-us/azure/architecture/best-practices/api-design) | Документация | 0.95 | REST patterns, versioning |
| 2 | [Zalando RESTful API Guidelines](https://opensource.zalando.com/restful-api-guidelines/) | Стандарт | 0.95 | Enterprise API standards |
| 3 | [Auth0: Authorization Code Flow](https://auth0.com/docs/get-started/authentication-and-authorization-flow/authorization-code-flow) | Документация | 0.95 | OAuth 2.0 flow |
| 4 | [JWT Best Practices - Curity](https://curity.io/resources/learn/jwt-best-practices/) | Руководство | 0.95 | JWT security |
| 5 | [JWT Security Best Practices 2025](https://jwt.app/blog/jwt-best-practices/) | Статья | 0.90 | RS256, expiration |
| 6 | [VAADATA: JWT Vulnerabilities & Attacks](https://www.vaadata.com/blog/jwt-json-web-token-vulnerabilities-common-attacks-and-security-best-practices/) | Статья | 0.90 | Algorithm confusion, none attack |
| 7 | [Stripe API Documentation](https://stripe.com/docs/api) | Документация | 0.95 | Versioning, error format |
| 8 | [GitHub API Documentation](https://docs.github.com/en/rest) | Документация | 0.95 | REST + GraphQL hybrid |
| 9 | [Twilio API Best Practices](https://www.twilio.com/docs/usage/rest-api-best-practices) | Документация | 0.95 | Deprecation policy |
| 10 | [GraphQL Best Practices](https://graphql.org/learn/best-practices/) | Документация | 0.95 | N+1, depth limiting |
| 11 | [RFC 8725: JWT Best Current Practices](https://datatracker.ietf.org/doc/html/rfc8725) | RFC | 0.95 | Official JWT security |
| 12 | [OWASP JWT Testing](https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/06-Session_Management_Testing/10-Testing_JSON_Web_Tokens) | Руководство | 0.95 | Security testing |

---

**Последняя верификация**: 2026-01-02
**Уровень достоверности**: high

---

*Проверено: 2026-01-09*
